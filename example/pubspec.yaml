name: example
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  pgsql_annotation: ^4.9.0

dev_dependencies:
  # Used by tests. Not required to use `pgsql_serializable`.
  _pgsql_serial_shared_test:
    path: ../shared_test

  # REQUIRED!
  build_runner: ^2.3.3

  # Used by tests. Not required to use `pgsql_serializable`.
  build_verify: ^3.0.0

  # REQUIRED!
  pgsql_serializable: ^6.8.0

  # Not required to use `pgsql_serializable`.
  path: ^1.8.0
  test: ^1.21.6
